name: example
description: Network Logger example project
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  dio: ^5.0.1
  
  network_logger:
    path: ..

flutter:
  uses-material-design: true
