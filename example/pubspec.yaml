name: example
description: Network Logger example project
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  dio: ^3.0.0
  
  network_logger:
    path: ..

flutter:
  uses-material-design: true
